DATA FOLDER — Put training text files (.txt) here.

Or use the downloader:
  python prepare_data.py --source sample       # Quick test data
  python prepare_data.py --source wikipedia     # Real knowledge
  python prepare_data.py --source tinystories   # Short stories
  python prepare_data.py --source all           # Everything

Files here are used when you run: python -m qor train
