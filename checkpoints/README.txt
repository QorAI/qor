CHECKPOINTS FOLDER — Trained models saved here automatically.

After training you'll see:
  best_model.pt          # Best model (lowest loss)
  checkpoint_XXXX.pt     # Periodic saves

Loaded automatically by: python -m qor chat / serve / generate
